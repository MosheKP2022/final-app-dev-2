<%= form_with(model: labeled_offering) do |form| %>
  <% if labeled_offering.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(labeled_offering.errors.count, "error") %> prohibited this labeled_offering from being saved:</h2>

      <ul>
        <% labeled_offering.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :offering_id %>
    <%= form.text_field :offering_id %>
  </div>

  <div class="field">
    <%= form.label :tag_id %>
    <%= form.text_field :tag_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
