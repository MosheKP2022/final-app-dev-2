<%= form_with(model: safe) do |form| %>
  <% if safe.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(safe.errors.count, "error") %> prohibited this safe from being saved:</h2>

      <ul>
        <% safe.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :offering_id %>
    <%= form.text_field :offering_id %>
  </div>

  <div class="field">
    <%= form.label :user %>
    <%= form.number_field :user %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
