console.log("let's edit the comment!")

$("#<%= dom_id(@comment) %>").remove();

var edited_comment = $("<%= j(render 'form', comment: @comment, id: "edit") %>");

$("#<%= dom_id(@comment.offering) %>_new_comment_form").before(edited_comment);